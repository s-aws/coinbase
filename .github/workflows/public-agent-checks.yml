name: Public Agent Checks

on:
  pull_request:
  push:
    branches:
      - main
      - master

jobs:
  agent-policy:
    name: Agent Policy And Regression Process
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install policy-check dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[test]"

      - name: Ownership and autonomous queue policy
        shell: pwsh
        run: |
          python tools/check_ownership.py
          python tools/run_autonomous_work_queue_check.py --summary-only

      - name: Admin API OpenAPI freshness
        shell: pwsh
        run: python tools/generate_admin_api_openapi.py --check

      - name: Admin API route inventory freshness
        shell: pwsh
        run: python tools/export_admin_api_route_inventory.py --check

      - name: Admin API controlled-live MVP route smoke
        shell: pwsh
        run: |
          python -m pytest `
            tests/regression/test_admin_api_contract.py::test_admin_api_order_live_execution_service_dependency_reads_decision_log `
            tests/regression/test_admin_api_contract.py::test_admin_api_manual_order_route_passes_backend_admission_to_command_service `
            tests/regression/test_admin_api_contract.py::test_admin_api_manual_order_route_executes_through_backend_runtime_dependencies `
            tests/regression/test_admin_api_contract.py::test_admin_api_manual_order_route_blocks_admitted_quote_above_backend_cap `
            -q --tb=short

      - name: Regression runner contract
        shell: pwsh
        run: |
          # Ordinary PRs use focused tests. Durable milestone closeout uses:
          # python tools/run_parallel_regression.py --workers 4
          python -m pytest tests/regression/test_parallel_regression_runner.py -v --tb=short

      - name: Continuous deployment contract
        shell: pwsh
        run: python -m pytest tests/regression/test_backend_cd_workflow.py -v --tb=short
